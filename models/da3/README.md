Place the local `DA3-SMALL` model directory here.

Expected default path:

`D:\agent_lunwen\code2\models\da3\DA3-SMALL`

The runtime expects a local directory that can be loaded by:

`DepthAnything3.from_pretrained(<model_dir>)`
